# requirements.txt
# Build dependencies for OpenDrug documentation.
# Install with: pip install -r requirements.txt
# =============================================================================
sphinx>=7.0,<9.0
sphinx-rtd-theme>=2.0
# 3.12.x triggers Python 3.13 native crashes when introspecting
# torch / torch_geometric; pin to a stable 2.x release for CI.
sphinx-autodoc-typehints>=2.0,<3.5
sphinx-copybutton>=0.5
myst-parser>=3.0
sphinx-togglebutton>=0.4
pygments>=2.17
